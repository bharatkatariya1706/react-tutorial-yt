import React from "react";

const UserContext = React.createContext()

export default UserContext
//we have to make a context provider along with the context by wrapping every component by <UserContext>

